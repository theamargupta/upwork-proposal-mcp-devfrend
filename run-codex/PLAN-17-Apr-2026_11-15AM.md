# PLAN — Claude Help Files for Upwork Job MCP

## Objective
Next.js 16 + MCP + Supabase dashboard for Upwork jobs (companion to the Chrome extension). Existing root CLAUDE.md (3.7KB) is solid. Add nested links, slash commands, subagents, hooks, per-area CLAUDE.md, and settings.

## Current State
- Root: `/Volumes/maersk/amargupta/Documents/Latest Projects/Portfolio Project/Upwork Job MCP`
- `CLAUDE.md` — keep, add Nested Context.
- `AGENTS.md` — standard reminder.
- No `.claude/`.
- Structure: `app/{(auth),(dashboard),api,oauth}`, `lib/{mcp,supabase,types.ts,utils.ts}`, `supabase/migrations/`.
- Tech: Next.js 16, React 19, MCP SDK, Supabase SSR, ShadCN, Framer Motion, Zod v4, `@base-ui/react`.
- MCP tools: list_jobs, get_job, search_jobs, update_job_status, get_top_jobs.
- Companion table `upwork_jobs` (produced by Chrome extension); this app adds `upwork_mcp_oauth_*` + `upwork_job_access_log`.

## Target State
```
CLAUDE.md                            # insert Nested Context
.claude/
  settings.local.json
  commands/
    add-mcp-tool.md
    add-migration.md
    job-status-transition.md
    oauth-flow-test.md
    access-log-check.md
  agents/
    mcp-tool-reviewer.md
    oauth-reviewer.md
    migration-reviewer.md
    job-status-state-machine.md
  hooks/
    block-env-commit.sh
    prevent-tailwind-config.sh
    lint-on-write.sh
app/CLAUDE.md
app/api/CLAUDE.md
lib/mcp/CLAUDE.md
lib/supabase/CLAUDE.md
components/ui/CLAUDE.md
supabase/CLAUDE.md
```

## Step-by-step Instructions

### Step 1 — Patch root CLAUDE.md
After `## Overview`, insert:

```markdown
## Nested Context
- `app/` — route groups, auth gate
- `app/api/` — REST + MCP route
- `lib/mcp/` — MCP server + tools + OAuth
- `lib/supabase/` — server / service-role / client
- `components/ui/` — ShadCN
- `supabase/` — migrations, RLS, access log
```

### Step 2 — `.claude/commands/`

**`add-mcp-tool.md`** — scaffold tool in `lib/mcp/tools/jobs.ts`, register in `lib/mcp/server.ts`, Zod v4 input, extract user id from OAuth context, log to `upwork_job_access_log`.

**`add-migration.md`** — next NNN after `002_oauth_access_log.sql`. RLS + `auth.uid()` scoping. Table names prefixed `upwork_` if new.

**`job-status-transition.md`** — output the allowed transition matrix (`new → maybe → applying → applied → skip`) and generate SQL to validate no invalid rows exist.

**`oauth-flow-test.md`** — curl steps to register client, run PKCE authorization, exchange token, call MCP.

**`access-log-check.md`** — SQL to view recent `upwork_job_access_log` rows, grouped by tool_name.

### Step 3 — `.claude/agents/`

**`mcp-tool-reviewer.md`** — Zod v4 inputs, OAuth user id from session, service-role client only, access log on every call, scoped queries by user_id.

**`oauth-reviewer.md`** — same as Memory MCP plan (PKCE S256, SHA-256 token hashes, redirect_uri validation, etc.).

**`migration-reviewer.md`** — NNN sequence, `upwork_` table prefix, RLS, FK indexes.

**`job-status-state-machine.md`:**
```markdown
---
name: job-status-state-machine
description: Ensures job status transitions follow new → maybe → applying → applied → skip.
tools: Read, Grep, Glob
---

Audit `update_job_status` tool and any UI action that updates `upwork_jobs.status`.

Allowed transitions:
- `new` → `maybe` | `applying` | `skip`
- `maybe` → `applying` | `skip` | `new`
- `applying` → `applied` | `skip`
- `applied` → `skip` (terminal otherwise)
- `skip` → `new` (allow un-skipping)

Flag any code that permits `applied` → `applying` or skips validation.

Report violations.
```

### Step 4 — `.claude/hooks/`
`block-env-commit.sh`, `prevent-tailwind-config.sh`, `lint-on-write.sh` — standard.

### Step 5 — Per-area CLAUDE.md

**`app/CLAUDE.md`** — route groups `(auth)`, `(dashboard)`, auth gate, server components default.

**`app/api/CLAUDE.md`** — MCP route stateless, OAuth required; REST routes (`/api/jobs/*`) use Zod + SSR client for dashboard calls, service-role only for admin ops; response shape `{ success, data }`.

**`lib/mcp/CLAUDE.md`** — tools in `tools/jobs.ts`, registered in `server.ts`, OAuth helpers in `oauth.ts`.

**`lib/supabase/CLAUDE.md`** — `server.ts` (SSR), `client.ts` (browser), `service-role.ts` (server-only with `import 'server-only'`).

**`components/ui/CLAUDE.md`** — ShadCN managed via CLI.

**`supabase/CLAUDE.md`** — migrations pattern, `upwork_` prefix, access log must match MCP tool names, RLS on every table.

### Step 6 — `.claude/settings.local.json`
```json
{
  "permissions": {
    "allow": [
      "Bash(npm run dev)",
      "Bash(npm run build)",
      "Bash(npm run lint)",
      "Bash(npm run start)",
      "Bash(npx tsc --noEmit*)",
      "Bash(npx eslint *)",
      "Bash(npx shadcn@latest add *)",
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git add *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      { "matcher": "Bash", "hooks": [{ "type": "command", "command": "bash .claude/hooks/block-env-commit.sh" }] },
      { "matcher": "Write", "hooks": [{ "type": "command", "command": "bash .claude/hooks/prevent-tailwind-config.sh" }] }
    ],
    "PostToolUse": [
      { "matcher": "Edit|Write", "hooks": [{ "type": "command", "command": "bash .claude/hooks/lint-on-write.sh" }] }
    ]
  }
}
```

### Step 7
`chmod +x .claude/hooks/*.sh`

## DO NOTs
- DO NOT modify source under `app/`, `lib/`, `components/`, `supabase/migrations/`.
- DO NOT change `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `components.json`.
- DO NOT create `tailwind.config.*`.
- DO NOT commit.

## Verification
```
ls .claude/commands .claude/agents .claude/hooks
ls app/CLAUDE.md app/api/CLAUDE.md lib/mcp/CLAUDE.md lib/supabase/CLAUDE.md components/ui/CLAUDE.md supabase/CLAUDE.md
node -e "JSON.parse(require('fs').readFileSync('.claude/settings.local.json','utf8'));console.log('ok')"
grep -q "## Nested Context" CLAUDE.md
```
